# Adding an Agency

The full procedure — processing the feed, config, refreshing, and the PMTiles rebuild step that's easy to skip and ships silently broken agencies — lives in [`PIPELINE_OPERATIONS.md`](PIPELINE_OPERATIONS.md) § Integrating a New Transit Agency. This file used to restate those steps separately (and had drifted stale in the process — it described editing `public/data/index.json` directly, which is no longer how the pipeline works); `PIPELINE_OPERATIONS.md` is now the single canonical version.

---

[Back to Data](./DATA.md)
